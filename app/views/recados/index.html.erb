<p id="notice"><%= notice %></p>

<h1>Recados</h1>

<%= link_to 'Escrever', new_recado_path, class: 'btn btn-outline-primary'  %>
<br>
<br>

  <table>
    <thead>
      <tr>
        <th></th>
      </tr>
    </thead>

    <tbody>
        <%# <tr> %>
          <% @recados.each do |recado| %>
              <%# <td> %>
                <tr>
                  <td>
                    <div class="card bg-warning mb-3" style="max-width: 14rem;">
                      <div class="card-body">
                        <p class="card-text">
                          <small class="text-muted"><b><cite title="Source Title"><%= recado.autor %></cite></b></small>
                        </p>
                        <p class="card-text"><b><%= recado.descricao %></b></p>
                        <%= link_to '...', edit_recado_path(recado), class: 'btn btn-outline-info' %>
                        <%= link_to 'X', recado, method: :delete, class: 'btn btn-outline-danger' %>
                    </div>
                  <td>
                </tr>
              <%# </td> %>
          <% end %>
        <%# </tr> %>
    </tbody>
  </table>

  <br>
